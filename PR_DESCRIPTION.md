🎯 **What:** Removed the unused `_fen` state variable and its updater function `setFen` from `src/App.tsx`. Refactored the initialization logic of the `game` object and utilized cloning (`new Chess(); clone.loadPgn(game.pgn())`) to correctly trigger React re-renders when mutations occur on the active `chess.js` game state.
💡 **Why:** `_fen` was declared but never actually used in the application. Calling `setFen(game.fen())` was previously utilized as an anti-pattern hack to force a re-render. Removing the unused state and properly cloning the mutated class instance ensures standard React state management and resolves ESLint/TypeScript warnings.
✅ **Verification:** Verified changes via `npm run lint`, built successfully using `npm run build`, and checked the core test suite utilizing `npx vitest run`. The app is verified not to crash on valid moves by verifying `chess.js` constructor constraints.
✨ **Result:** Improved component initialization, eliminated dead variables, and implemented a proper paradigm for managing class-based third-party instances inside a React state.