# 🧪 Add missing test for invalid move in Tutorial

## 🎯 What
Added missing error test for invalid move in the Tutorial component.

## 📊 Coverage
Tested invalid move handling in the `handleMove` function within `Tutorial.tsx`, ensuring that it safely catches errors without throwing exceptions that crash the application.

## ✨ Result
Improved test coverage and reliability of the Tutorial component by validating its error boundary handling around domain logic execution.
