Title: 🧹 [code health: replace empty catch block with warn in Tutorial.tsx]

Description:
* 🎯 **What:** Replaced the empty catch block inside the `handleMove` function in `src/components/Tutorial.tsx` with a `console.warn('Invalid move attempted in tutorial');` statement. We also used optional catch binding (`catch { ... }`) to avoid unused variable linting errors.
* 💡 **Why:** Empty catch blocks are universally discouraged as they swallow potential bugs silently. Adding a simple console warning prevents this anti-pattern and adheres to standard code quality metrics (e.g., eslint's `no-empty` rule) while remaining low-risk since it maintains the existing behavior of gracefully ignoring bad moves.
* ✅ **Verification:** Verified via `read_file` that the changes were correctly applied to the file. Ran `npm run lint` and `npm run build` which succeeded, proving that no typescript or build regressions were introduced. Requested code review which approved the change as safe and fully ready for merge.
* ✨ **Result:** Improved maintainability by removing an anti-pattern. Developers will now see warnings in the console if an exception is thrown during tutorial moves, facilitating easier debugging without breaking the application's flow.